import "./Loader.css"; // Import your styles here

const Loader = () => {
  return (
    <div className="loader-container">
      <span className="loader-text">Loading</span>
    </div>
  );
};

export default Loader;
